// Este arquivo é gerado automaticamente a partir do schema do Supabase.
// Rode `npm run db:types` (com SUPABASE_PROJECT_ID configurado) após aplicar as
// migrations em supabase/migrations/ para substituir este placeholder pelos tipos reais.
//
// Enquanto isso, usamos `any` para não travar o build — o schema real está
// 100% definido em supabase/migrations/*.sql.

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
